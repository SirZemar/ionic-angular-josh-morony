import { BigAlertDirective } from './big-alert.directive';

describe('BigAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new BigAlertDirective();
    expect(directive).toBeTruthy();
  });
});
